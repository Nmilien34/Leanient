// Header padding sits on top of the modal safe-area inset (ModalSafeArea), so it
// only needs the small in-content gap the other modal headers use.
export const ACCOUNT_HEADER_TOP_PADDING = 6;
export const ACCOUNT_PROFILE_TOP_PADDING = 16;
